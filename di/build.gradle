description = 'Dependency injection for Spark'

ext {
	projectName = 'nightfall-di'
}

jar {
	appendix = 'di'
}

apply from: "$rootDir/shadow-jar.gradle"

dependencies {
	provided libs.SPARK, libs.COMMONS_CLI, libs.SLF4J
	compile  libs.JSON, libs.GOVERNATOR
	testCompile libs.TEST
}
